.title {
    display: flex;
    align-items: center;
}

.title-text {
    font-size: $h2;
    line-height: 1;
}

.title-icon {
    background: svg-load('spectr.svg', fill='#7A66CC', width=100%, height=100% ) center no-repeat;
    width: 65px;
    height: 35px;
    margin-right: 30px;
}